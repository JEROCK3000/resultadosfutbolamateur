Options -Indexes
RewriteEngine On

# Redirigir todo al front controller (index.php)
# excepto archivos y directorios que realmente existen (assets, etc.)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]
